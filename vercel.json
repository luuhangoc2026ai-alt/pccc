{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "npm --prefix pccc-web run build",
  "outputDirectory": "pccc-web/dist",
  "installCommand": "npm --prefix pccc-web install",
  "framework": "vite",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
